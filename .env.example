VITE_APP_NAME=Chess Coach
# Disable Tailwind native (Lightning CSS) to avoid native binary issues on some
# Node/OS setups (e.g., Node 22 on Windows). Tailwind falls back to JS.
TAILWIND_DISABLE_NATIVE=1

